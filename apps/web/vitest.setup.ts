import "@testing-library/jest-dom/vitest";

/**
 * Vitest global setup for @ibn-hayan/web.
 *
 * The jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.) are
 * registered globally so that test files can use them without per-file
 * imports.
 */
